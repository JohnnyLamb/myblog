{
  "name": "myblog",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal static publishing pipeline.",
  "scripts": {
    "build": "node build.js --clean",
    "dev": "node build.js --clean --watch --serve",
    "serve": "node build.js --serve"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2",
    "highlight.js": "^11.9.0"
  },
  "engines": {
    "node": ">=18"
  }
}
